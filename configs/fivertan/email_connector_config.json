{
  "config": {
    "append_file_option": "${file_option}",
    "compression": "${compression}",
    "delimiter": "${delimiter}",
    "empty_header": false,
    "external_id": "string",
    "file_type": "${file_type}",
    "on_error": "fail",
    "schema": "${schema}",
    "table": "${table}"
  },
  "group_id": "${group_id}",
  "pause_after_trial": "${pause_after_trial}",
  "paused": "${paused}",
  "run_setup_tests": "${run_setup_tests}",
  "service": "email",
  "sync_frequency": "${sync_frequency}",
  "trust_certificates": true,
  "trust_fingerprints": true
}
