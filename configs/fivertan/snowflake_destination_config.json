{
    "group_id": "${group_id}",
    "service": "snowflake",
    "region": "${region}",
    "time_zone_offset": "-5",
    "config": {
      "host": "${snowflake_host}",
      "port": 443,
      "database": "${snowflake_database}",
      "auth": "PASSWORD",
      "user": "${snowflake_user}",
      "password": "${snowflake_password}"
    }
}