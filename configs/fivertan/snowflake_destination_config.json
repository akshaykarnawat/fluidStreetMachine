{
  "config": {
    "auth": "PASSWORD",
    "database": "${snowflake_database}",
    "host": "${snowflake_host}",
    "password": "${snowflake_password}",
    "port": 443,
    "user": "${snowflake_user}"
  },
  "group_id": "${group_id}",
  "region": "${region}",
  "service": "snowflake",
  "time_zone_offset": "-5"
}
